# 클라이언트 피드백 분석 및 수정 계획

## 📋 피드백 요약

### 1. **13-1번 질문 데이터가 web2에 표시되지 않는 문제**

**현상**: 13-1번 질문(식이조절 세부 문항)이 DB에는 저장되지만 web2의 상담 기록 페이지에 표시되지 않음

**원인 분석**:

- `web1/src/component/CounselingRequestForm.js`에서 상담 요청 폼에 `preferredDate`와 `preferredTime` 필드가 없음
- 현재는 `timeSlot`(상담 가능 시간)만 저장하고 있음
- `web2/src/pages/CounselingRecordPage.js`에서는 `request.preferredDate`와 `request.preferredTime`을 표시하려고 하지만 데이터가 없음

**수정 계획**:

1. `CounselingRequestForm.js`에 희망 상담 날짜/시간 입력 필드 추가
2. 저장 시 `preferredDate`, `preferredTime` 필드도 함께 저장
3. `CounselingRecordPage.js`에서 해당 필드가 없을 때 적절한 처리

**관련 파일**:

- `web1/src/component/CounselingRequestForm.js` (수정 필요)
- `web2/src/pages/CounselingRecordPage.js` (확인 필요)

---

### 2. **상세보기 접근 경로 개선 (3클릭 → 2클릭)**

**현상**: 환자 대시보드 > 환자목록 > 상담요청 > 요청됨 버튼 클릭 시 상세보기까지 3번 클릭 필요

**요구사항**:

- 환자 대시보드 > 환자목록 > 상담요청 > "요청됨" 버튼 클릭 시 바로 상담 기록 상세보기로 이동

**수정 계획**:

1. `DashboardPage.js`에서 상담 상태 컬럼의 "요청됨" 상태 버튼/링크를 클릭하면 해당 환자의 최신 상담 요청 상세보기로 바로 이동
2. 최신 상담 요청 ID를 찾아서 `/counseling-record/{requestId}`로 라우팅

**관련 파일**:

- `web2/src/pages/DashboardPage.js` (수정 필요)

---

### 3. **상담 노트 중복 문제**

**현상**:

- 상담 기록 페이지(`CounselingRecordPage.js`)의 상담 노트
- 환자 상세 페이지(`PatientCounseling.jsx`)의 상담 노트
- 두 곳에서 같은 용도로 사용되는 것 같아 중복 느낌

**현재 구조**:

- `CounselingRecordPage.js`: `counselingRequests/{requestId}/notes` (상담 요청별 노트)
- `PatientCounseling.jsx`: `patients/{patientId}/notes` (환자별 노트)

**수정 계획**:

1. 용도 명확화 필요 - 클라이언트와 협의하여 각각의 용도 정의
2. 옵션 A: 상담 요청별 노트는 해당 상담 요청에만 관련된 내용, 환자별 노트는 환자 전체에 대한 종합 기록
3. 옵션 B: 하나로 통합 (예: 환자별 노트만 사용하고 상담 요청 페이지에서도 환자별 노트 표시)
4. 옵션 C: UI 개선으로 중복 느낌 제거 (예: 환자 상세 페이지에서 상담 요청별 노트를 링크로 연결)

**관련 파일**:

- `web2/src/pages/CounselingRecordPage.js` (검토 필요)
- `web2/src/pages/patient-detail/PatientCounseling.jsx` (검토 필요)

---

### 4. **네이게이션바 사라지는 문제**

**현상**: 네이게이션바에서 "보관 환자"를 클릭하면 네이게이션바가 사라짐 (크롬에서 확인)

**원인 분석**:

- `ArchivedPatientsPage.js`가 `Layout` 컴포넌트를 사용하지 않아서 네비게이션바가 표시되지 않음
- 다른 페이지들은 `Layout` 컴포넌트로 감싸져 있지만, `ArchivedPatientsPage.js`는 독립적으로 렌더링됨

**수정 계획**:

1. `ArchivedPatientsPage.js`를 `Layout` 컴포넌트로 감싸기
2. 다른 페이지들과 동일한 레이아웃 구조로 통일

**관련 파일**:

- `web2/src/pages/ArchivedPatientsPage.js` (수정 필요)

---

### 5. **대시보드 그래프 점수 표시 개선**

**현상**: 대시보드 막대 그래프에서 각 문항별 평균 점수가 아니라 환자 점수만 표시됨

**요구사항**:

- 그래프 꼭대기 중앙에 점수 표시 형식으로 변경

**현재 구조**:

- `DashboardPage.js`에는 암 종류별/위험도별 통계 그래프만 있음 (환자 수 기준)
- 환자별 점수 그래프는 `PatientHealthStatus.js`에 있음

**수정 계획**:

1. `PatientHealthStatus.js`의 막대 그래프에 점수를 막대 꼭대기에 표시하도록 수정
2. Chart.js의 `datalabels` 플러그인 사용 또는 커스텀 렌더링
3. 각 막대 위에 해당 점수 값 표시

**관련 파일**:

- `web2/src/pages/patient-detail/PatientHealthStatus.js` (수정 필요)

---

## 🎯 수정 우선순위

1. **높음 (즉시 수정 필요)**

   - 네이게이션바 사라지는 문제 (#4) - 사용성에 직접적 영향
   - 13-1번 질문 데이터 표시 문제 (#1) - 데이터 누락 문제

2. **중간 (개선 필요)**

   - 상세보기 접근 경로 개선 (#2) - UX 개선
   - 대시보드 그래프 점수 표시 (#5) - 가독성 개선

3. **낮음 (협의 필요)**
   - 상담 노트 중복 문제 (#3) - 클라이언트와 용도 협의 후 결정

---

## 📝 수정 작업 체크리스트

### 1단계: 즉시 수정 항목

- [ ] 네이게이션바 문제 수정 (`ArchivedPatientsPage.js`에 Layout 적용)
- [ ] 13-1번 질문 데이터 표시 문제 확인 및 수정
  - [ ] `CounselingRequestForm.js`에 preferredDate/preferredTime 필드 추가
  - [ ] 저장 로직 수정
  - [ ] `CounselingRecordPage.js`에서 표시 확인

### 2단계: UX 개선 항목

- [ ] 상세보기 접근 경로 개선
  - [ ] `DashboardPage.js`에서 상담 상태 버튼 클릭 시 상세보기로 이동 로직 추가
  - [ ] 최신 상담 요청 ID 조회 로직 추가
- [ ] 대시보드 그래프 점수 표시 개선
  - [ ] `PatientHealthStatus.js`의 막대 그래프에 점수 표시 추가
  - [ ] Chart.js 플러그인 또는 커스텀 렌더링 적용

### 3단계: 협의 후 결정

- [ ] 상담 노트 중복 문제 해결 방안 결정
  - [ ] 클라이언트와 용도 협의
  - [ ] 선택한 방안에 따라 수정

---

## 🔍 추가 확인 사항

1. **13-1번 질문 관련**:

   - 13-1번 질문이 상담 요청 폼의 어떤 필드와 관련이 있는지 확인 필요
   - "13-1번 질문이 DB 저장은 되는데 web2에 안 뜨는" 문제가 정확히 어떤 데이터를 말하는지 확인 필요
   - 상담 요청 폼의 "희망 상담 날짜/시간" 필드가 13-1번 질문과 관련이 있는지 확인 필요

2. **상담 노트 중복**:

   - 현재 두 가지 노트 시스템의 실제 사용 패턴 확인
   - 클라이언트가 원하는 통합 방안 확인

3. **그래프 점수 표시**:
   - "각 문항별 평균 점수"가 정확히 무엇을 의미하는지 확인
   - 환자 개별 점수만 표시하는 것이 의도된 것인지, 아니면 평균 점수도 함께 표시해야 하는지 확인

---

## 📌 참고사항

- 모든 수정 사항은 테스트 후 배포 필요
- web3 통합 전에 이 수정 사항들을 완료하는 것이 좋음
- 크롬 브라우저에서 특히 네비게이션바 문제가 발생한다고 하니, 크롬에서 테스트 필수
